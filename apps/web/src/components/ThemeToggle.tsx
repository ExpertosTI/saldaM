'use client';

import { useTheme } from 'next-themes';
import { useEffect, useState } from 'react';

// ThemeToggle is disabled to enforce Dark Mode premium theme
export default function ThemeToggle() {
    return null;
}
