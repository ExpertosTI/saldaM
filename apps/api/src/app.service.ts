/**
 * AppService removed - was dead code returning only "Hello World!"
 * Health check moved to AppController.healthCheck()
 *
 * If shared business logic is needed across modules,
 * create specific services in their respective modules.
 */
export {};
